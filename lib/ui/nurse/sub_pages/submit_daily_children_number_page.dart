import 'package:flutter/material.dart';

class SubmitDailyChildrenPage extends StatelessWidget {
  const SubmitDailyChildrenPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}